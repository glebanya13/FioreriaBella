<Page x:Class="FioreriaBella.MVVM.Views.ManageUsersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="clr-namespace:FioreriaBella.MVVM.Converters"
      Title="ManageUsers"
      x:Name="ManageUsers">

  <Page.Resources>
    <conv:BoolToStringConverter x:Key="BoolToStringConverter"/>
  </Page.Resources>

  <Grid Background="Transparent">
    <Border Padding="30"
            BorderBrush="Goldenrod"
            BorderThickness="2"
            CornerRadius="16"
            Width="820"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="#FFFDF5F0"
            SnapsToDevicePixels="True">

      <StackPanel>

        <Button Content="← Назад"
                Command="{Binding BackCommand}"
                Style="{StaticResource ElegantButtonStyle}"
                Width="100"
                Margin="0,0,0,15"
                HorizontalAlignment="Left" />

        <TextBlock Text="Управление пользователями"
                   FontSize="24"
                   FontWeight="Bold"
                   FontFamily="Georgia"
                   Foreground="DarkOliveGreen"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <Button Content="＋ Новый пользователь"
                Command="{Binding AddUserCommand}"
                Style="{StaticResource ElegantButtonStyle}"
                Width="200"
                Margin="0,0,0,15"
                HorizontalAlignment="Left" />

        <DataGrid x:Name="UsersGrid"
                  ItemsSource="{Binding Users}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionUnit="Cell"
                  HeadersVisibility="Column"
                  RowHeight="80"
                  ColumnHeaderHeight="30"
                  Margin="0,0,0,10"
                  Style="{StaticResource ElegantDataGridStyle}">

          <DataGrid.Columns>

            <DataGridTemplateColumn Header="ID" Width="50">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Id}" 
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Логин" Width="130">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Username}" 
                             TextWrapping="Wrap"
                             Padding="4"
                             FontFamily="Palatino Linotype"
                             Foreground="#4B2E2B"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Email" Width="*">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Email}"
                             TextWrapping="Wrap"
                             Padding="4"
                             FontFamily="Palatino Linotype"
                             Foreground="#4B2E2B"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Админ" Width="80">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding IsAdmin, Converter={StaticResource BoolToStringConverter}}"
                             HorizontalAlignment="Center"
                             FontWeight="SemiBold"
                             Foreground="DarkGreen"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Действия" Width="100">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Width="30" Height="30"
                            Margin="3"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Редактировать"
                            Command="{Binding DataContext.EditCommand, ElementName=ManageUsers}"
                            CommandParameter="{Binding}">
                      <Image Source="pack://application:,,,/MVVM/Resources/edit_icon.png" Width="18" Height="18"/>
                    </Button>

                    <Button Width="30" Height="30"
                            Margin="3"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Удалить"
                            Command="{Binding DataContext.DeleteCommand, ElementName=ManageUsers}"
                            CommandParameter="{Binding}">
                      <Image Source="pack://application:,,,/MVVM/Resources/delete_icon.png" Width="18" Height="18"/>
                    </Button>
                  </StackPanel>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

          </DataGrid.Columns>
        </DataGrid>

      </StackPanel>
    </Border>
  </Grid>
</Page>
